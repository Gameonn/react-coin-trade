import React from "react";
import "./OrbitSpinner.css";

const OrbitSpinner = (props) => {
  return (
    <div className="orbit-spinner">
        <div className="orbit"></div>
        <div className="orbit"></div>
        <div className="orbit"></div>
    </div>
  );
};

export default OrbitSpinner;
